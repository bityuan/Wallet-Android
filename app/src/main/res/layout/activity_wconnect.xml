<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <include
        android:id="@+id/inc_loading"
        layout="@layout/fragment_wc_loading" />

    <include
        android:id="@+id/inc_proposal"
        layout="@layout/fragment_session_proposal"
        android:visibility="gone" />

    <include
        android:id="@+id/inc_proposaled"
        layout="@layout/fragment_session_proposaled"
        android:visibility="gone" />

    <include
        android:id="@+id/inc_request"
        layout="@layout/fragment_session_request"
        android:visibility="gone" />


</RelativeLayout>